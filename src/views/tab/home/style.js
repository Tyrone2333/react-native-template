import {
    Platform,
    StyleSheet,
    Text,
    View,
    Button,
    TouchableWithoutFeedback,
} from 'react-native'

const styles = StyleSheet.create({
    btn: {
        backgroundColor: '#f00',
    },
})

export default styles
